# Dev image: ts-node-dev for hot reload (used by skaffold dev + file sync)
# rebuild: common 1.0.58 (multi-seat refund email total)
FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm install
COPY . .
CMD ["npm", "start"]
