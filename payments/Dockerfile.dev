# Dev image: ts-node-dev for hot reload (used by skaffold dev + file sync)
# cachebust: #11 connect routes (new files — force rebuild, sync can miss them)
FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm install
COPY . .
CMD ["npm", "start"]
