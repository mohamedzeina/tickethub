# Dev image: ts-node-dev for hot reload (used by skaffold dev + file sync)
# bump: #9 per-card ratings batch route (force rebuild — new file sellers.ts)
FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm install
COPY . .
CMD ["npm", "start"]
