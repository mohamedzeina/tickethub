# Dev image: ts-node-dev for hot reload (used by skaffold dev + file sync)
# rev: bake idempotent (upsert / dup-tolerant) replica listeners
FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm install
COPY . .
CMD ["npm", "start"]
