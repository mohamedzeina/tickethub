# Dev image: ts-node-dev for hot reload (used by skaffold dev + file sync)
# cachebust: rate-limit bypass header (e2e) — force rebuild so the new src bakes in
FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm install
COPY . .
CMD ["npm", "start"]
