# Dev image: ts-node-dev for hot reload (used by skaffold dev + file sync)
# cachebust: #11 phase2 clean rebuild (prior build during disk-full storm was corrupt)
FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json ./
RUN npm install
COPY . .
CMD ["npm", "start"]
